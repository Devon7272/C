/* Demonstrates a simple for statment that initializes element of a 1,000 element-array to to the value of 50 */
#include <stdio.h>
int count;

int main( void )
{
    // this for statment will assign the value 50 to each element in a array
    for (count = 0; count < 1000; array[count++] = 50)
    {
        // putting a semicolon into the block make it more obvious that there is no work being done inthe body of the
        // for statment
        ;
    }

}
