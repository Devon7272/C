/* Demonstrates a simple for statment by sorting through arrays */
#include <stdio.h>

int count;

int main ( void )
{
    // prints Sorting through array...
    count = 1; 
    for ( printf("Now sorting the array...") ; count < 1000; count++)
        /* Sorting statments here */

    return 0;
}
